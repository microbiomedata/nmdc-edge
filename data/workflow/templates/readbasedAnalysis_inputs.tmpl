{
    "main_workflow.enabled_tools": <%- JSON.stringify(enabled_tools) %>,
    "main_workflow.reads": <%- JSON.stringify(reads) %>,
    "main_workflow.paired": <%= paired %>,
    "main_workflow.prefix": "<%= prefix %>",
    "main_workflow.outdir": "<%= outdir %>"
}