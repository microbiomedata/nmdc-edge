[cromwell]
cromwell_url = "http://localhost:8088/api/workflows/v1"
cromwell_api = "http://localhost:9999"

[directories]
stage_dir = "/path/to/stage/dir"
template_dir = "/path/to/template/dir"
data_dir = "/tmp"
raw_dir = "/path/to/raw/data/files"

[site]
resource = "NERSC-Perlmutter"
site = "Processing Site"

[nmdc]
url_root = "https://data.microbiomedata.org/data/"
api_url = "http://localhost:8000"

[state]
watch_state = "State File"
#agent_state = "/tmp/agent.state"  Commenting this out will default to _state/agent.state
activity_id_state = "/Path/to/activity_id_state"

[workflows]
workflows_config = "workflows.yaml"

[credentials]
client_id = "sys0wm66"
client_secret = "O8Q!64t,^xy:"

[jaws]
jaws_config = "jaws-test.conf"
jaws_token = "jaws-test-token.conf"
